파일열기
문자열 읽어서 불러오기

문자열을 줄별로 잘라서 배열에 저장하기
strArray 배열에 원본 문자열을 저장 

String 구조체 맴버:str 하나뿐인
String strArray
String GstrArray 선언

GNGGAcheck 함수 만들어서, strArray 의 원소인 문자열중에, GNGGA로 시작하는 문자열을 추출,
그 추출한 문자열을 GstrArray에 저장

GstrArray를 하나씩 꺼내서
N, E 등의 알파벳을 기준으로 parsing 하는 함수를 작성 : charCheck()

--checksum이 맞는가? 를 체크하는 함수

Location 배열 l에 위도 경도 고도 를 저장

l 배열을 읽어서 평균 등의 값을 계산


